---
import Container from "./Container.astro";
import LinkButton from "./LinkButton.astro";
import { logicalPathname, normalizePath, sitePath } from "../../lib/site";

const currentPath = logicalPathname(Astro.url.pathname);

function navItemIsActive(href: string) {
  const h = normalizePath(href);
  if (h === "/") return currentPath === "/";
  return currentPath === h || currentPath.startsWith(`${h}/`);
}

const nav = [
  { href: "/services", label: "Services" },
  { href: "/case-studies", label: "Case studies" },
  { href: "/about", label: "About" },
] as const;

const navLinkDesktop =
  "relative rounded-lg px-3 py-2 text-sm font-medium transition-colors";
const navLinkDesktopIdle =
  "text-muted-foreground hover:text-foreground";
const navLinkDesktopActive = "text-foreground";

const navLinkMobile =
  "rounded-xl px-3 py-3.5 text-sm font-medium transition-colors";
const navLinkMobileIdle =
  "text-muted-foreground hover:bg-accent/60 hover:text-foreground";
const navLinkMobileActive = "bg-accent/70 text-foreground";
---

<header
  class="sticky top-0 z-40 border-b border-border/50 bg-background/75 backdrop-blur-xl supports-[backdrop-filter]:bg-background/65"
>
  <input type="checkbox" id="mobile-nav-toggle" class="peer sr-only" />
  <Container wide class="flex h-[4.25rem] items-center justify-between gap-4">
    <a
      href={sitePath("/")}
      class="group inline-flex min-w-0 shrink-0 items-center gap-3"
    >
      <span
        class="grid size-9 shrink-0 place-items-center rounded-2xl bg-gradient-to-br from-primary to-[oklch(0.38_0.12_210)] text-primary-foreground shadow-md shadow-primary/25 ring-1 ring-white/10"
      >
        <span class="font-heading text-base font-bold">V</span>
      </span>
      <span class="font-heading truncate text-base font-semibold tracking-tight"
        >Vedamit</span
      >
    </a>

    <div class="flex shrink-0 items-center gap-1 md:gap-2">
      <nav class="hidden items-center gap-0.5 md:flex" aria-label="Primary">
        {
          nav.map((item) => {
            const active = navItemIsActive(item.href);
            return (
              <a
                href={sitePath(item.href)}
                class:list={[
                  navLinkDesktop,
                  active ? navLinkDesktopActive : navLinkDesktopIdle,
                ]}
                aria-current={active ? "page" : undefined}
              >
                <span class="relative z-10">{item.label}</span>
                {active ? (
                  <span
                    class="absolute inset-x-2 bottom-1 h-0.5 rounded-full bg-primary/90"
                    aria-hidden="true"
                  />
                ) : null}
              </a>
            );
          })
        }
      </nav>
      <div class="hidden md:ml-2 md:block">
        <LinkButton
          href="/contact"
          variant="primary"
          size="sm"
          class={navItemIsActive("/contact")
            ? "ring-2 ring-primary/40 ring-offset-2 ring-offset-background"
            : undefined}
          ariaCurrent={navItemIsActive("/contact") ? "page" : undefined}
        >
          Get started
        </LinkButton>
      </div>

      <label
        id="mobile-nav-menubutton"
        for="mobile-nav-toggle"
        class="nav-menu-btn inline-flex size-11 cursor-pointer items-center justify-center rounded-xl border border-transparent text-muted-foreground transition-colors hover:border-border hover:bg-accent/50 hover:text-foreground md:hidden"
        aria-label="Menu"
        aria-controls="mobile-nav-panel"
        aria-expanded="false"
        aria-haspopup="true"
      >
        <svg
          class="icon-menu size-6"
          aria-hidden="true"
          xmlns="http://www.w3.org/2000/svg"
          fill="none"
          viewBox="0 0 24 24"
          stroke-width="1.5"
          stroke="currentColor"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5"></path>
        </svg>
        <svg
          class="icon-close absolute left-1/2 top-1/2 size-6 -translate-x-1/2 -translate-y-1/2"
          aria-hidden="true"
          xmlns="http://www.w3.org/2000/svg"
          fill="none"
          viewBox="0 0 24 24"
          stroke-width="1.5"
          stroke="currentColor"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            d="M6 18 18 6M6 6l12 12"></path>
        </svg>
      </label>
    </div>
  </Container>

  <div
    class="grid max-h-0 overflow-hidden border-border/50 bg-background/95 backdrop-blur-xl transition-[max-height] duration-300 ease-out peer-checked:max-h-[32rem] peer-checked:border-b md:hidden"
    id="mobile-nav-panel"
    role="region"
    aria-labelledby="mobile-nav-menubutton"
  >
    <Container wide class="flex flex-col gap-1 pb-5 pt-2">
      <nav aria-label="Mobile primary" class="flex flex-col gap-0.5">
        {
          nav.map((item) => {
            const active = navItemIsActive(item.href);
            return (
              <a
                href={sitePath(item.href)}
                class:list={[
                  navLinkMobile,
                  active ? navLinkMobileActive : navLinkMobileIdle,
                ]}
                aria-current={active ? "page" : undefined}
              >
                {item.label}
              </a>
            );
          })
        }
      </nav>
      <div class="mt-3 border-t border-border/60 pt-4">
        <LinkButton
          href="/contact"
          variant="primary"
          size="sm"
          class:list={[
            "w-full",
            navItemIsActive("/contact")
              ? "ring-2 ring-primary/40 ring-offset-2 ring-offset-background"
              : undefined,
          ]}
          ariaCurrent={navItemIsActive("/contact") ? "page" : undefined}
        >
          Get started
        </LinkButton>
      </div>
    </Container>
  </div>
</header>

<script>
  function syncMobileNavAria() {
    const toggle = document.getElementById(
      "mobile-nav-toggle",
    ) as HTMLInputElement | null;
    const btn = document.getElementById("mobile-nav-menubutton");
    if (toggle && btn)
      btn.setAttribute("aria-expanded", toggle.checked ? "true" : "false");
  }

  function closeMobileNav() {
    const toggle = document.getElementById(
      "mobile-nav-toggle",
    ) as HTMLInputElement | null;
    if (toggle) toggle.checked = false;
    syncMobileNavAria();
  }

  document.addEventListener("astro:page-load", () => {
    closeMobileNav();
    syncMobileNavAria();
  });

  document.addEventListener("change", (e) => {
    if ((e.target as HTMLInputElement | null)?.id !== "mobile-nav-toggle")
      return;
    syncMobileNavAria();
  });

  document.addEventListener("click", (e) => {
    const panel = document.getElementById("mobile-nav-panel");
    if (!panel?.contains(e.target as Node)) return;
    if ((e.target as HTMLElement).closest("a")) closeMobileNav();
  });
</script>

<style is:global>
  .nav-menu-btn {
    position: relative;
  }
  .nav-menu-btn .icon-close {
    display: none;
  }
  #mobile-nav-toggle:checked + div .nav-menu-btn .icon-menu {
    display: none;
  }
  #mobile-nav-toggle:checked + div .nav-menu-btn .icon-close {
    display: block;
  }
</style>
