# Deploy the Apps Script in scripts/google-sheets-contact-webapp.gs, then paste the Web app URL:
PUBLIC_CONTACT_SHEET_WEBAPP_URL=

# Optional: GitHub repo URL for `npm run deploy` when this project has no `origin` remote (or Pages lives in another repo).
# GHPAGES_REPO=https://github.com/you/your-pages-repo.git
